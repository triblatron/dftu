PROJECT( DFTU )
CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )

ADD_LIBRARY( dftu SHARED src/dftu.cpp )
TARGET_COMPILE_DEFINITIONS( dftu PUBLIC DFTU_LIBRARY )
TARGET_INCLUDE_DIRECTORIES( dftu PUBLIC include )
